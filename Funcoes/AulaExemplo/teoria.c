/*
FUNÇÕES:
 - Define um nome de acordo com o que a funcao sera responsavel (VERBO)
 - São métodos;
 - Define o programa em blocos de codigos;
 - Há retorno (irá retornar um dado - int, float, double, struct...);
 - Parâmetro;

PROCEDIMENTOS:
 - Define um nome de acordo com o que o procedimento sera responsavel (VERBO)
 - É uma função sem RETORNO (void), porém pode ter Parâmetro;
 
    Exemplificando:
        -- INT é o tipo de retorno
        -- Soma( parâmetro ) é o nome da função


        int soma( int n1, int n2) {
            int soma = n1 + n2;  //variável local
            return soma;
         }
            
 */
